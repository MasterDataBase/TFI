This exe will update the first 8 decoder for each Titan edge decoder.
Basedon on the configuration provided in the file 'DecoderToChange.json'

In that file is necessary to insert the debug, 
    if true, the exe will not deploy anything but will print only a json file for each decoder found, 
    if false it will print and apply the config
Is also necessary to inser the address ip of the device to change